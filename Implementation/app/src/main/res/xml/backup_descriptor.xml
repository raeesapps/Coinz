<?xml version="1.0" encoding="utf-8"?>

<!--The backup descriptor. Contains the path to the shared preferences key-value store-->
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude
        domain="sharedpref"
        path="FeatureCollection_Shared_Prefs.xml" />
</full-backup-content>
